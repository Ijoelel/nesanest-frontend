import { tienne } from "@/lib/font";

export default function RestaurantPage() {
    return (
        <div className="w-full h-full flex justify-center items-center mt-96">
            <p className={`text-6xl font-bold ${tienne.className}`}>
                Coming Soon . . .
            </p>
        </div>
    );
}
